import 'package:flutter/material.dart';

class AppTheme {
  static Color backgroundColor = Color(0xff2b3242);
  static Color primaryColor = Colors.deepOrange;
  static Color hintColor = Colors.blueGrey;
  static Color secondaryColor = Colors.white;
  static ThemeMode dark = ThemeMode.dark;
  static ThemeMode light = ThemeMode.light;
  static Color focusColor = Colors.redAccent;
}
